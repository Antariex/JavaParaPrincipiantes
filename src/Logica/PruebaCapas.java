package Logica;
import IGU.Pantalla;

public class PruebaCapas {
    public static void main(String[] args) {
        Pantalla visual = new Pantalla();
        visual.setVisible(true);
        visual.setLocationRelativeTo(null);
    }
}
